config.stopBubbling = true

# Lets @RequiredArgsConstructor carry @Qualifier from the field onto the constructor parameter,
# so Spring can tell the password encoder and the PIN encoder apart.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
